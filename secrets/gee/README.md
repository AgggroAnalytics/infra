# GEE credentials (legacy / optional)

GEE credentials are now managed in **aggro-field-worker** via SOPS, not in infra or k8s.

See [aggro-field-worker/secrets/README.md](../../aggro-field-worker/secrets/README.md).

This directory is unused unless you keep a local copy of the JSON here for reference.
